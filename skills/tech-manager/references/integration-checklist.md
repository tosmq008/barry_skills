# 前后端联调检查清单

## 概述

本文档提供前后端联调的完整检查清单，确保前后端接口对接正确、数据交互完整。

---

## 1. 接口契约检查

### 1.1 API 路径与方法

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 路径一致 | 前端调用路径与后端定义一致 | [ ] |
| 方法一致 | GET/POST/PUT/DELETE 方法匹配 | [ ] |
| 版本一致 | API 版本号匹配 (如 /api/v1/) | [ ] |

### 1.2 请求参数

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 参数名称 | 前后端参数名完全一致 | [ ] |
| 参数类型 | 数据类型匹配 (string/number/boolean) | [ ] |
| 必填参数 | 必填参数都已传递 | [ ] |
| 参数格式 | 日期、枚举等格式一致 | [ ] |

### 1.3 响应数据

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 响应结构 | 数据结构符合契约定义 | [ ] |
| 字段名称 | 字段名前后端一致 | [ ] |
| 数据类型 | 返回数据类型正确 | [ ] |
| 空值处理 | null/undefined 处理正确 | [ ] |

---

## 2. 连通性检查

### 2.1 基础连通

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 网络可达 | 前端能访问后端服务 | [ ] |
| 端口正确 | 后端服务端口配置正确 | [ ] |
| 协议正确 | HTTP/HTTPS 协议匹配 | [ ] |

### 2.2 跨域配置 (CORS)

| 检查项 | 说明 | 状态 |
|--------|------|------|
| Origin 允许 | 前端域名在允许列表 | [ ] |
| Methods 允许 | 请求方法被允许 | [ ] |
| Headers 允许 | 自定义请求头被允许 | [ ] |
| Credentials | Cookie/认证信息传递正确 | [ ] |

### 2.3 认证授权

| 检查项 | 说明 | 状态 |
|--------|------|------|
| Token 传递 | 认证 Token 正确传递 | [ ] |
| Token 格式 | Bearer Token 格式正确 | [ ] |
| 权限验证 | 接口权限验证正常 | [ ] |
| 过期处理 | Token 过期处理正确 | [ ] |

---

## 3. 数据流检查

### 3.1 数据提交流程

```
前端表单 ──▶ 数据序列化 ──▶ API请求 ──▶ 后端接收 ──▶ 数据解析 ──▶ 业务处理
```

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 表单数据收集 | 所有字段正确收集 | [ ] |
| 数据序列化 | JSON 序列化正确 | [ ] |
| Content-Type | 请求头类型正确 | [ ] |
| 后端解析 | 数据正确解析 | [ ] |

### 3.2 数据查询流程

```
前端请求 ──▶ 后端查询 ──▶ 数据返回 ──▶ 前端解析 ──▶ 数据展示
```

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 查询参数 | 分页、筛选参数正确 | [ ] |
| 数据返回 | 返回数据完整 | [ ] |
| 数据解析 | 前端正确解析数据 | [ ] |
| 数据展示 | 页面正确渲染数据 | [ ] |

### 3.3 文件上传流程

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 文件格式 | 支持的文件类型正确 | [ ] |
| 文件大小 | 大小限制配置正确 | [ ] |
| 上传方式 | multipart/form-data 正确 | [ ] |
| 进度反馈 | 上传进度显示正确 | [ ] |

---

## 4. 错误处理检查

### 4.1 错误响应格式

**标准错误响应：**
```json
{
  "code": 40001,
  "message": "参数错误",
  "details": {
    "field": "email",
    "reason": "格式不正确"
  }
}
```

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 错误码统一 | 前后端错误码定义一致 | [ ] |
| 错误信息 | 错误信息清晰可读 | [ ] |
| 错误详情 | 详细错误信息可用 | [ ] |

### 4.2 HTTP 状态码

| 状态码 | 场景 | 前端处理 |
|--------|------|----------|
| 200 | 成功 | 正常处理 |
| 400 | 参数错误 | 显示错误提示 |
| 401 | 未认证 | 跳转登录 |
| 403 | 无权限 | 显示无权限提示 |
| 404 | 资源不存在 | 显示不存在提示 |
| 500 | 服务器错误 | 显示系统错误 |

### 4.3 网络异常

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 超时处理 | 请求超时正确处理 | [ ] |
| 断网处理 | 网络断开正确提示 | [ ] |
| 重试机制 | 失败重试逻辑正确 | [ ] |

---

## 5. 功能完整性检查

### 5.1 CRUD 操作

| 操作 | 前端 | 后端 | 联调 |
|------|------|------|------|
| 创建 (Create) | [ ] | [ ] | [ ] |
| 读取 (Read) | [ ] | [ ] | [ ] |
| 更新 (Update) | [ ] | [ ] | [ ] |
| 删除 (Delete) | [ ] | [ ] | [ ] |

### 5.2 分页与筛选

| 检查项 | 说明 | 状态 |
|--------|------|------|
| 分页参数 | page/pageSize 正确传递 | [ ] |
| 总数返回 | total 字段正确返回 | [ ] |
| 筛选条件 | 筛选参数正确处理 | [ ] |
| 排序功能 | 排序参数正确处理 | [ ] |

### 5.3 边界场景

| 场景 | 前端处理 | 后端处理 | 状态 |
|------|----------|----------|------|
| 空数据 | 显示空状态 | 返回空数组 | [ ] |
| 大数据量 | 虚拟滚动/分页 | 分页查询 | [ ] |
| 并发请求 | 防重复提交 | 幂等处理 | [ ] |
| 特殊字符 | 正确编码 | 正确解码 | [ ] |

---

## 6. 性能检查

| 检查项 | 标准 | 状态 |
|--------|------|------|
| 接口响应时间 | < 500ms | [ ] |
| 页面加载时间 | < 3s | [ ] |
| 首屏渲染时间 | < 1.5s | [ ] |
| 接口并发数 | 合理控制 | [ ] |

---

## 7. 联调问题记录

| 问题ID | 描述 | 类型 | 责任方 | 状态 |
|--------|------|------|--------|------|
| INT_001 | [描述] | [前端/后端/双方] | [责任方] | [状态] |

---

## 8. 联调完成确认

### 签字确认

| 角色 | 确认人 | 日期 | 签字 |
|------|--------|------|------|
| 前端开发 | | | [ ] |
| 后端开发 | | | [ ] |
| 技术经理 | | | [ ] |

### 完成标准

- [ ] 所有接口联调通过
- [ ] 所有功能点验证通过
- [ ] 无阻塞性问题
- [ ] 错误处理完善
- [ ] 性能达标
